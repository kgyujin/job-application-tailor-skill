---
name: job-application-tailor
description: 채용공고를 분석해 이력서, 자기소개서, 포트폴리오를 해당 공고에 특화되도록 수정한다. 여러 채용공고를 한 번에 받아 사용자의 직무/경력과의 적합도 순위를 매기는 것도 지원한다. GitHub 계정(public/private 레포)을 스캔해 이력서/포트폴리오용 프로젝트 초안을 md로 정리하는 것도 지원한다. 사용자가 채용공고 링크/텍스트와 기존 지원 서류(이력서, 자소서, 포트폴리오)를 제공하고 "이 공고에 맞게 수정해줘", "공고 맞춤형으로 다듬어줘", "이 공고들 중에 나한테 맞는 순서대로 정리해줘", "여러 공고 우선순위 매겨줘", "내 GitHub 레포에서 포트폴리오에 쓸 프로젝트 정리해줘" 등을 요청할 때 사용한다.
license: MIT
---

# 채용공고 맞춤형 지원서류 튜닝 (Job Application Tailor)

채용공고에 맞춰 이력서, 자기소개서, 그리고 포트폴리오를 다시 쓰거나, 여러 채용공고를
사용자의 직무/경력과 대조해 지원 우선순위를 매기거나, GitHub 레포에서 포트폴리오용
프로젝트 초안을 뽑아내는 스킬이다.
Claude Code와 Codex(codex-cli) 양쪽에서 동일하게 이 파일 하나를 읽고 그대로 따르면 된다.

## 세 가지 모드

- **모드 A — 단일 공고 튜닝**: 공고 1개 + 서류를 받아 그 공고에 맞게 서류를 다시 쓴다. (1~7단계)
- **모드 B — 다중 공고 순위 매기기**: 공고 2개 이상을 받아 사용자의 직무/경력사항과
  대조해 적합도 순으로 정리한다. (0단계) 순위가 나온 뒤 사용자가 특정 공고를 골라
  튜닝을 요청하면 그 공고에 대해 모드 A(1~7단계)를 이어서 수행한다.
- **모드 C — GitHub 프로젝트 자동 정리**: `gh` CLI로 사용자의 GitHub 레포(public/private)를
  스캔해 이력서/포트폴리오에 쓸 만한 프로젝트를 골라 `resume-markdown-editor` md 계약
  형식의 초안 파일로 정리한다. (G1~G6단계) 산출물은 초안일 뿐이며, 사용자가 검토하고 보완한
  뒤 모드 A의 5단계(포트폴리오 조정) 입력이나 모드 B의 사용자 프로필(기술 스택) 보완
  자료로 이어 쓸 수 있다.

요청에 공고가 여러 개 포함되어 있으면 먼저 모드 B로 순위를 매긴 뒤, 사용자가 원하면
상위 공고부터 모드 A로 넘어간다. 공고가 1개뿐이면 0단계를 건너뛰고 바로 1단계로 간다.
요청에 "GitHub 스캔/정리"가 포함되어 있으면 모드 C를 먼저 수행해 초안을 만들고,
사용자가 초안을 승인하면 그 초안을 들고 모드 A 또는 모드 B로 이어간다.

## 절대 원칙

1. **사실은 유지하되, 자기소개서의 표현 강화는 허용한다.** 합격 가능성을 높이기 위한
   "수습 가능한 수준의 과장"은 실제 경험의 핵심을 더 선명하고 자신감 있게 표현하는 데
   한정한다. 관련 경험을 앞세우고, 임팩트 있는 동사를 쓰고, 질문과 무관한 불리한 세부사항을
   줄이는 것은 허용한다. 다만 면접에서 상황, 본인 역할, 행동, 결과를 설명할 수 있고 원본
   자료와 사실관계가 맞아야 하며, 필요한 경우 사용자의 확인 내용으로 근거를 보완한다.
   - 허용: 실제로 수행한 일의 중요도와 기여를 명확히 드러내기, 실제 팀 성과에 대한 본인
     기여를 역할 중심으로 강조하기, 그리고 의미가 바뀌지 않는 범위에서 수치를 반올림하거나
     범위로 표현하기.
   - 금지: 없는 경력, 기술, 자격, 수치, 기간, 직함을 만들기, 실제보다 큰 책임 범위나 숙련도를
     주장하기, 팀 성과를 개인 단독 성과로 바꾸기, 직접 수행하지 않은 일을 했다고 쓰기, 그리고
     질문이 요구한 사실을 거짓으로 답하거나 숨겨 오해를 유도하기.
   이 예외는 자기소개서 문장 표현에만 적용한다. 이력서, 경력기술서, 포트폴리오, GitHub
   초안, 요구사항 매칭, 적합도 채점은 확인된 사실을 그대로 사용한다.
2. **원본을 덮어쓰지 않는다.** 항상 새 파일(버전 접미사 포함)로 저장하고, 원본은 그대로 남긴다.
3. **불확실한 사실은 채워 넣지 말고 사용자에게 되묻는다.** 공고 요구사항과 매칭되는 경험이
   원본 서류에 없으면, 임의로 지어내지 말고 "이 부분은 확인이 필요합니다"라고 표시한다.
   자기소개서의 표현을 더 강하게 만들려는 경우에도 직접 수행 범위, 팀과 개인의 기여,
   수치, 기간, 그리고 기술 숙련도가 불명확하면 먼저 확인하고, 확인 전에는 보수적인 표현을 쓴다.
   순위 매기기에서 판단에 필요한 정보(연봉, 근무지, 사용자의 선호 등)가 없으면 해당
   항목은 "정보 없음"으로 두고 점수에서 제외하거나 중립으로 처리한다 — 임의로 추정하지 않는다.
4. **사용자의 기존 문체와 언어를 유지한다.** 원본이 한국어면 한국어로, 영어면 영어로 작성한다.
5. **(모드 C) GitHub 데이터는 로컬 인증 세션으로만 읽는다.** 토큰을 코드나 파일에 직접
   적지 않고 항상 `gh auth login`으로 인증된 로컬 세션을 사용하며, 레포 내용을 외부
   서비스로 전송하지 않는다. 비공개(private) 레포는 회사/팀 소유이거나 NDA 대상일 수
   있으므로, 공개 가능 여부를 반드시 사용자에게 확인한 뒤 md 초안에 포함한다.
6. **나열 표현은 자연스럽게 정리한다.** 지원서류 본문, 분석 결과, 사용자에게 보여주는
   보고에서 단순한 항목을 나열할 때 `·`를 기본 구분자로 반복하지 않는다.
   - 짧은 명사, 기술, 키워드 열거는 문맥에 맞게 쉼표(`,`)나 슬래시(`/`)로 구분하고, 두 항목은 `및`이나 `와/과`로 연결한다.
   - 항목이 세 개 이상이면 앞 항목을 쉼표로 구분하고 마지막 항목 앞에만 `그리고`를 둔다.
     예: `문제 정의, 데이터 분석, 협업, 그리고 실행`
   - 두 항목은 `A와 B` 또는 `A 및 B`, 선택지나 조합은 `A/B`처럼 쓴다. `그리고`를 항목
     사이마다 반복하거나 구분 기호를 불필요하게 섞지 않는다.
   - `·`는 원본 양식이나 짧은 메타데이터처럼 공간 절약이 실제로 필요한 경우에만 제한적으로
     사용한다.
   - 각 항목에 설명, 근거, 상태, 체크가 붙어야 하면 번호, 불릿 또는 표를 사용할 수 있다.
     절차용 목록, 검토 체크리스트, 순위표, G6 md 계약의 `성과` 목록처럼 형식이 정해진
     영역은 해당 형식을 우선한다.

## 자기소개서 추가 작성 원칙

### 인상적인 도입부

- 도입부는 사용자의 실제 행동과 직무 역량을 압축한 짧은 비유, 이미지, 그리고 문장으로 작성할 수 있다.
- 비유를 사용하더라도 첫 문장 또는 바로 다음 문장에서 핵심 역량과 지원 직무의 연결을 명확히 한다. 인상만 남기고 근거가 늦게 나오는 도입은 사용하지 않는다.
- 특정 인물, 노래, 그리고 작품의 표현을 모방하거나 인용하지 않는다. 사용자의 경험에서만 고유한 이미지를 만든다.
- 사용자가 예시로 제시한 인상적인 문장도 그대로 복사하지 않는다. 예시의 구조나 인상을 참고하더라도 표현과 이미지는 사용자의 경험에서 새로 만든다.
- 도입부의 자기평가는 뒤에 이어지는 실제 상황, 본인 역할, 행동, 그리고 결과로 증명할 수 있어야 한다.

### 두괄식 구성

- 각 자기소개서 문항의 첫 문장은 질문에 대한 답 또는 핵심 역량을 직접 제시한다.
- 이후 문단은 `상황/과제 → 본인이 맡은 역할 → 행동과 판단 → 결과 → 지원 직무에서의 기여` 순서로 전개한다.
- 분량이 짧은 문항은 배경 설명과 일반적인 포부를 줄이고, 첫 문장의 주장과 이를 증명하는 경험을 우선한다.
- 인상적인 비유를 도입부에 사용한 경우에도 바로 뒤에 평이한 문장으로 핵심 주장을 풀어 써 두괄식 흐름을 유지한다.

### 본인 수행 범위 구분

- 자기소개서에는 사용자가 직접 수행한 업무 가운데 본인이 판단하고 검증한 내용만 경험으로 서술한다.
- 팀의 결과는 팀의 성과로, 본인의 기여는 맡은 역할과 행동으로 분리해 쓴다. 팀이 해결한 일을 본인이 해결했다고 확대하지 않는다.
- 원본 자료에서 작업 분담이나 직접 구현 여부가 확인되지 않는 항목은 본문에서 제외하거나 `본인 확인 필요`로 남긴다.

### 자기소개서 표현 강화 기준

- 실제 경험을 더 설득력 있게 보이도록 관련 맥락을 앞세우고, 역할 중심 동사와 성과 중심 문장을 사용할 수 있다.
- `담당`, `주도`, `리딩`, `총괄`, `전문가`, `핵심`처럼 책임 범위나 숙련도를 높여 보이는 표현은 그 수준의 의사결정, 실행, 조정, 그리고 결과를 면접에서 설명할 수 있을 때만 사용한다.
- 질문이 직접 요구하지 않는 불리한 세부사항을 장황하게 강조하지 않아도 된다. 다만 경력 연차, 자격 여부, 사용 기술, 사용 기간, 그리고 수치처럼 질문이 요구하는 사실은 정확히 답한다.
- 표현을 강화한 문장은 `상황 → 본인 역할 → 행동과 판단 → 결과`를 구체적으로 설명할 수 있는지 최종 확인한다.

## 입력으로 필요한 것

### 모드 A (단일 공고 튜닝)

- 채용공고 원문(텍스트 붙여넣기 또는 URL) — 회사명, 직무명, 자격요건, 우대사항, 주요 업무, 회사/팀 키워드를 포함
- 기존 이력서 파일 경로
- 기존 자기소개서 파일 경로 (있다면)
- 기존 포트폴리오 파일/링크 (있다면)

이 중 하나라도 없으면 먼저 사용자에게 요청한다. 채용공고 URL만 있고 접근이 안 되면
사용자에게 텍스트를 붙여넣어 달라고 요청한다.

### 모드 B (다중 공고 순위 매기기)

- 채용공고 2개 이상 (텍스트 붙여넣기 또는 URL 목록)
- 사용자의 직무/경력 프로필 — 다음 중 하나로 확보한다.
  - 기존 이력서 또는 경력기술서 파일 경로 (있으면 이걸 우선 사용)
  - 파일이 없으면 사용자에게 총 경력 연차, 보유 기술 스택, 주요 도메인, 희망 직무/방향을
    직접 물어본다
- (선택) 사용자의 우선순위 가중치 — 예: "연봉보다 기술 스택 성장이 더 중요하다",
  "재택 근무 가능 여부가 중요하다" 등. 없으면 기본 가중치(아래 0단계 참조)를 사용한다.

이력서/경력 프로필이 전혀 없고 사용자가 제공을 거부하면, 순위를 매기지 않고
공고별 요구사항 요약표까지만 제공한 뒤 판단에 필요한 정보를 요청한다.

### 모드 C (GitHub 프로젝트 자동 정리)

- `gh` CLI가 로컬에 설치되어 있고 `gh auth login`으로 인증되어 있어야 한다. 없으면
  설치 및 인증 방법을 안내하고 완료될 때까지 스캔을 진행하지 않는다.
- 스캔 대상 GitHub 사용자명 — 명시하지 않으면 `gh api user`로 확인되는 현재 인증
  계정을 기본값으로 쓰되, 다른 사람 계정을 스캔하려는 것인지 확인한다.
- **레포 접근 범위(Public만 / Private만 / 둘 다)** — 반드시 사용자에게 확인한다.
  요청에 이미 명시되어 있으면(예: "public 레포만") 그 값을 사전 입력값으로 받아들이고
  재확인 없이 진행하되, 결과 보고에 어떤 범위로 스캔했는지 명시한다. 명시되지 않았다면
  진행 전에 반드시 질문한다.
- (선택) 조직(organization) 소유 레포까지 포함할지 여부 — 기본은 개인 계정 레포만.

범위 확인 없이 임의로 "일단 public만 본다" 또는 "전부 다 본다"고 넘겨짚지 않는다.

## 작업 절차

### 0단계 — 다중 공고 순위 매기기 (모드 B, 공고 2개 이상일 때만)

1. **공고별 요구사항 추출**: 각 공고에 대해 1단계와 동일하게 회사/직무, 필수 자격요건,
   우대사항, 주요 업무, 기술 스택, 근무조건(위치, 고용형태, 그리고 연봉이 명시된 경우)을 추출한다.
2. **사용자 프로필 정리**: 이력서/경력기술서에서 총 경력 연차, 보유 기술 스택, 주요
   도메인/산업 경험, 최근 역할과 시니어리티, (자소서가 있다면) 커리어 방향성 키워드를
   뽑는다. 파일이 없으면 사용자 답변으로 대체한다.
3. **공고별 적합도 채점**: 공고마다 아래 5개 항목을 0~100점 척도로 채점하고 가중 평균한다.
   세부 채점 기준은 `reference/ranking-criteria.md`를 따른다.

   | 항목 | 기본 가중치 | 내용 |
   |---|---|---|
   | 필수 자격요건 충족률 | 40% | must-have 중 실제 근거로 매칭되는 비율 |
   | 우대사항 충족률 | 20% | nice-to-have 중 매칭되는 비율 |
   | 직무 범위 적합도 | 15% | 주요 업무가 사용자의 실제 경험/희망 방향과 겹치는 정도 |
   | 경력 연차 부합도 | 10% | 공고가 요구하는 연차 밴드와 사용자 연차의 일치도 |
   | 근무조건 적합도 | 15% | 위치/고용형태/연봉 등 명시된 조건과 사용자 선호의 일치도 (정보 없으면 이 항목은 제외하고 나머지 항목 가중치를 비례 재배분한다) |

   사용자가 별도 가중치를 지정하면 그 값을 우선 적용하고, 어떤 가중치를 썼는지 결과에
   명시한다.
4. **순위표 작성**: 총점 기준 내림차순으로 정렬하고, 아래 구간으로 등급을 표시한다.
   - 80점 이상: 강력 추천
   - 60~79점: 추천
   - 40~59점: 보류 (gap이 있어 보완이나 확인이 필요)
   - 40점 미만: 비추천 (필수 자격요건 다수 미충족)
5. **근거와 gap 명시**: 각 공고의 점수 옆에 핵심 근거(강하게 매칭된 항목 1~2개)와
   gap(미충족 필수 요건)을 함께 적는다. 점수만 던지지 않는다.
6. **다음 행동 제안**: 순위표 하단에 "N위 이내 공고부터 지원서류 튜닝을 진행할지" 사용자
   에게 확인한다. 사용자가 특정 공고를 지목하면 그 공고에 대해 1단계부터 모드 A를 수행한다.

공고 수가 많아(대략 15개 초과) 표가 지나치게 길어지면, 상위/중위/하위로 구간을 나누어
요약하고 전체 표는 별도 파일로 저장할 수 있다고 제안한다.

### G1단계 — 환경 확인 (모드 C)

1. `gh --version`으로 `gh` CLI 설치 여부를 확인한다. 없으면 https://cli.github.com
   설치 안내를 제공하고 설치가 확인될 때까지 다음 단계로 진행하지 않는다.
2. `gh auth status`로 인증 여부와 스코프를 확인한다. 미인증이면 `gh auth login` 안내
   후 완료될 때까지 대기한다.
3. 사용자가 Private 레포 스캔을 원하는데 인증 스코프가 private 접근을 포함하지 않으면,
   이 사실을 알리고 (a) public만 진행할지 (b) `gh auth refresh -s repo`로 스코프를
   추가할지 사용자에게 선택하게 한다. 임의로 스코프를 확장하지 않는다.

### G2단계 — 스캔 범위 확정 (모드 C)

1. 대상 GitHub 사용자명을 확인한다. 기본값은 `gh api user --jq .login`으로 얻은
   현재 인증 계정이다.
2. 앞서 "모드 C 입력" 절에서 정한 Public/Private/둘 다 범위를 최종 확인한다.
3. 조직 레포 포함 여부를 확인한다(기본: 미포함).

### G3단계 — 레포 목록 수집과 1차 필터링 (모드 C)

1. `gh repo list {대상} --visibility {public|private|all} --limit 300 --json name,description,isPrivate,isFork,isArchived,pushedAt,createdAt,primaryLanguage,stargazerCount,url`
   로 레포 목록을 가져온다.
2. 아래를 1차로 제외 후보로 표시한다(완전 삭제가 아니라 "제외 후보"로 남겨 G5에서
   사용자가 최종 판단하게 한다).
   - fork이면서 원본 대비 추가 커밋이 없는 레포
   - 커밋이 거의 없는 빈 레포/템플릿 레포
   - dotfiles, 설정 전용 등 "프로젝트"로 보기 애매한 레포
3. 세부 판단 기준은 `reference/github-scan-criteria.md`를 따른다.

### G4단계 — 후보 레포 심화 조사 (모드 C)

남은 후보 레포마다 다음을 조사한다.

- README 내용 요약 (`gh api repos/{owner}/{repo}/readme --jq .content` → base64 디코딩)
- 언어 비중 (`gh api repos/{owner}/{repo}/languages`)
- 본인 기여 비중 (`gh api repos/{owner}/{repo}/stats/contributors`) — 컨트리뷰터가
  2인 이상인데 본인 커밋 비중이 낮으면 "기여 비중 낮음, 실제 담당 범위 확인 필요"로
  표시한다. 지어내지 않는다.
- 최근 활동일(`pushedAt`)과 생성일(`createdAt`)로 진행 기간을 추정한다.

### G5단계 — 후보 요약표 제시 및 확정 (모드 C)

1. 레포명, 기간(생성일~최근 활동일), 공개 여부, 주 언어, 본인 기여 비중, 추천 사유
   또는 제외 사유를 표로 정리해 사용자에게 먼저 보여준다. **자동으로 전부 포함하지
   않는다** — 어떤 레포를 최종 초안에 넣을지 사용자 확인을 받는다.
2. Private 레포가 포함된 항목에는 "회사/팀 소유이거나 NDA 대상인지 확인이 필요합니다"
   경고를 함께 표시한다.
3. 기여 비중이 낮게 나온 팀 프로젝트는 "실제 담당 범위를 확인해달라"고 별도로 묻는다.

### G6단계 — md 초안 생성 (모드 C)

1. G5에서 확정된 레포만으로 `resume-markdown-editor`의 md 계약 형식에 맞춰 초안을
   작성한다.

   ```md
   ---
   document: portfolio
   name: (사용자 확인 필요)
   github: https://github.com/{대상}
   ---

   ## 프로젝트

   ### {레포명}
   기간: {createdAt 연월} - {pushedAt 연월}
   설명: (README 요약 기반, 근거 없는 미사여구 추가 금지)
   기술: {languages API 결과 상위 언어}
   링크: {repo url}
   성과:
   - (README/릴리스 노트 등에 명시된 근거가 있을 때만 기입, 없으면 "성과: 본인 확인 필요"로 남김)
   ```

2. 언어 통계를 모아 `## 기술 스택` 섹션도 함께 초안으로 만든다(그룹 구분이 애매하면
   그룹 없이 전체 목록으로 둔다 — 임의로 "Core/Platform" 같은 구분을 지어내지 않는다).
3. 수치와 성과는 레포/README/릴리스 노트 등에서 확인된 사실만 적는다. 확인되지 않으면
   빈 자리로 남기고 "본인 확인 필요"라고 표시한다(모드 A/B의 절대 원칙 1과 동일).
4. 파일명 `github_프로젝트_초안_{YYYYMMDD}.md`로 저장한다. 기존 포트폴리오 파일이
   있어도 덮어쓰지 않고 별도 초안 파일로 저장한다.

### 1단계 — 채용공고 분석

공고에서 다음을 추출해 표로 정리한다.

- 회사/팀, 직무명, 채용 배경(신규/충원/확장 등 알 수 있는 경우)
- 필수 자격요건 (must-have)
- 우대사항 (nice-to-have)
- 주요 업무 (수행할 일)
- 기술 스택 / 툴 키워드 (ATS 매칭에 쓰일 키워드)
- 회사가 반복해서 쓰는 표현과 가치관 키워드 (예: "주도적", "데이터 기반", "빠른 실행")

### 2단계 — 기존 서류와 매칭

- 공고 요구사항 각 항목에 대해 기존 서류에서 대응되는 경험/프로젝트/기술을 찾는다.
- 강하게 매칭되는 항목, 약하게 매칭되는 항목, 아예 없는 항목(gap)으로 분류한다.
- gap 항목은 지어내지 않고 사용자에게 "관련 경험이 있는지" 질문하거나, 정말 없으면
  이력서에서 억지로 채우지 않는다.

### 3단계 — 이력서 수정

- 공고 키워드와 겹치는 기술/경험을 상단 요약(있다면) 또는 강조 위치로 재배치한다.
- 불릿포인트 순서를 공고 우선순위에 맞게 재정렬한다(관련성 높은 경험을 위로).
- 기존에 있는 정량적 성과(수치)는 최대한 살리고, 공고에서 중요하게 보는 지표 형태로
  표현을 다듬는다(예: 매출 기여 → % / 금액, 성능 개선 → 배수 / 시간 단축).
- ATS 스캔을 고려해 공고에 나온 정확한 기술/툴 명칭(철자, 대소문자, 약어 vs 풀네임)을
  기존 사실과 일치하는 범위 내에서 맞춘다.
- 원본 포맷(양식, 폰트, 섹션 구조)은 유지한다. 내용과 순서만 조정한다.

### 4단계 — 자기소개서 수정

- 회사명/직무명/팀명을 정확히 반영한다.
- 공고의 핵심 요구사항 2~3개를 골라 각각에 대응하는 실제 경험 1개씩을 구체적으로 연결한다.
- 공고에서 반복되는 가치관/문화 키워드를 자연스럽게 녹인다(단, 기계적 키워드 나열 금지).
- 첫 문장은 문항의 답 또는 핵심 역량을 제시하고, 필요할 때 실제 행동을 압축한 독창적인 도입부를 사용한다. 비유 뒤에는 곧바로 핵심 주장과 근거를 붙인다.
- 구조 예시: (핵심 주장/지원 동기) → (직무 요구사항과 연결된 핵심 경험 1~2개, 상황-본인 역할-행동-결과) →
  (입사 후 기여 방향) → (마무리)
- 자기소개서의 표현은 위의 표현 강화 기준에 따라 더 자신감 있게 다듬을 수 있다. 다만 경력, 기술, 수치, 직함, 소유권, 그리고 책임 범위는 면접에서 설명 가능한 사실을 넘겨 쓰지 않는다.
- 팀이 수행한 업무와 사용자가 직접 맡은 업무를 구분해 서술한다. 직접 수행 여부가 불명확한 작업은 본문에 넣지 않는다.
- 글자수/분량 제한이 명시된 공고라면 반드시 그 제한을 지킨다.

### 5단계 — 포트폴리오 조정

- 공고 기술 스택과 가장 관련 있는 프로젝트를 최상단/최우선으로 배치할 것을 제안한다.
- 프로젝트 설명에서 공고와 관련된 기술적 의사결정, 사용 스택, 성과를 앞으로 끌어온다.
- 관련성이 낮은 프로젝트는 삭제 제안이 아니라 "우선순위 하향"으로 제안한다(사용자 최종 결정).

### 6단계 — 산출물 저장

- 원본 파일명 기준으로 아래 규칙으로 새 파일을 만든다.
  - `이력서.md` → `이력서_{회사명}_{직무명}.md`
  - `자기소개서.md` → `자기소개서_{회사명}_{직무명}.md`
  - 포트폴리오는 별도 조정 메모 파일 `포트폴리오_조정메모_{회사명}_{직무명}.md`로 정리
    (포트폴리오 원본 포맷이 md가 아닐 경우 원본은 건드리지 않고 조정 지침만 문서로 남긴다)
- 각 산출물 상단에 다음 메타 정보를 남긴다.

```
- 대상 공고: {회사명} / {직무명}
- 원본 파일: {원본 경로}
- 작성일: {오늘 날짜}
- 주요 변경사항: {요약 3줄 이내}
```

### 7단계 — 검토 체크리스트

최종 제출 전 다음을 확인하고 사용자에게 보고한다.

- [ ] 공고 필수 자격요건이 서류 어딘가에 명시적으로 드러나는가
- [ ] 지어낸 경험이나 수치가 없고, 경력, 기술, 그리고 직함을 부풀리지 않았는가 (원본과 대조 확인)
- [ ] 자기소개서의 표현 강화가 실제 역할과 결과를 벗어나지 않으며, 면접에서 설명 가능한가
- [ ] 직접 수행한 업무, 본인이 판단하고 검증한 범위, 그리고 팀의 결과를 구분했는가
- [ ] 각 문항이 핵심 주장부터 시작하는 두괄식인가
- [ ] 인상적인 도입부가 실제 경험으로 증명되며, 특정 인물의 표현을 모방하지 않았는가
- [ ] 회사명/직무명 오탈자가 없는가
- [ ] 분량/글자수 제한을 지켰는가
- [ ] 매칭되지 않는 gap 항목을 사용자에게 별도로 알렸는가

## 출력 형식

### 모드 A (단일 공고 튜닝)

작업 완료 시 다음을 함께 보고한다.

1. 공고 요구사항 요약표
2. 매칭/갭 분석 결과
3. 변경된 파일 목록과 경로
4. 원본 대비 핵심 변경 포인트 3~7개 (짧은 열거는 문장으로, 독립적인 근거와 조치가 필요할 때만 번호나 불릿으로 정리)
5. 사용자 확인이 필요한 gap 항목

### 모드 B (다중 공고 순위 매기기)

1. 공고별 요구사항 요약표 (회사/직무/필수요건/우대사항/근무조건)
2. 적합도 순위표 (총점, 등급, 항목별 점수, 핵심 근거, gap) — 총점 내림차순
3. 사용한 가중치 (기본값 또는 사용자 지정값)
4. 정보 부족으로 채점하지 못한 항목과 그 이유
5. 다음 단계 제안 (어떤 공고부터 모드 A로 넘어갈지 질문)

### 모드 C (GitHub 프로젝트 자동 정리)

1. 스캔 범위 (대상 계정, Public/Private/둘 다, 조직 레포 포함 여부)
2. 후보 레포 요약표와 제외한 레포 및 사유
3. 생성된 md 초안 파일 경로
4. 사용자 확인이 필요한 항목 (비공개 레포 공개 가능 여부, 기여 비중이 낮은 프로젝트,
   성과 수치를 채우지 못한 항목)
5. 다음 단계 제안 (이 초안을 resume-markdown-editor에서 다듬을지, 모드 A 5단계로
   이어가 기존 포트폴리오에 반영할지, 모드 B의 기술 스택 정보로 쓸지 질문)

## 참고 자료

- `reference/checklist.md` — 모드 A 7단계 체크리스트의 상세 버전 (긴 점검이 필요할 때 참조)
- `reference/ranking-criteria.md` — 모드 B 적합도 채점 기준의 상세 버전 (항목별 세부 배점,
  경계 사례 판단 기준)
- `reference/github-scan-criteria.md` — 모드 C 레포 포함/제외 판단 기준, `gh` 명령어
  목록, md 필드 매핑표의 상세 버전
